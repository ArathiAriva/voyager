# Agent guide — Voyager backend

Architecture and conventions for this service live in [.agents/backend.md](../.agents/backend.md) (overview: [.agents/AGENTS.md](../.agents/AGENTS.md)).

Quick start:

```bash
source .venv/bin/activate
bash scripts/run.sh --profile rand   # dev server on :8060
```
